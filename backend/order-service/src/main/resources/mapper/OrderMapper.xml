<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.hazenix.order.mapper.OrderMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into orders (number,status,user_id,address_book_id,order_time,checkout_time,pay_method,pay_status,amount,remark,phone,address,user_name,consignee,cancel_reason,rejection_reason,cancel_time,estimated_delivery_time,delivery_status,delivery_time,pack_amount,tableware_number,tableware_status)
        values (#{number},#{status},#{userId},#{addressBookId},#{orderTime},#{checkoutTime},#{payMethod},#{payStatus},#{amount},#{remark},#{phone},#{address},#{userName},#{consignee},#{cancelReason},#{rejectionReason},#{cancelTime},#{estimatedDeliveryTime},#{deliveryStatus},#{deliveryTime},#{packAmount},#{tablewareNumber},#{tablewareStatus})
    </insert>

    <update id="update" parameterType="top.hazenix.order.domain.entity.Orders">
        update orders
        <set>
            <if test="cancelReason != null and cancelReason!='' ">
                cancel_reason=#{cancelReason},
            </if>
            <if test="rejectionReason != null and rejectionReason!='' ">
                rejection_reason=#{rejectionReason},
            </if>
            <if test="cancelTime != null">
                cancel_time=#{cancelTime},
            </if>
            <if test="payStatus != null">
                pay_status=#{payStatus},
            </if>
            <if test="payMethod != null">
                pay_method=#{payMethod},
            </if>
            <if test="checkoutTime != null">
                checkout_time=#{checkoutTime},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="deliveryTime != null">
                delivery_time = #{deliveryTime}
            </if>
        </set>
        where id = #{id}
    </update>
    <update id="updateBatch">
        <foreach collection="list" item="item" separator=";">
            UPDATE orders
            <set>
                <if test="item.cancelReason != null and item.cancelReason != ''">
                    cancel_reason = #{item.cancelReason},
                </if>
                <if test="item.rejectionReason != null and item.rejectionReason != ''">
                    rejection_reason = #{item.rejectionReason},
                </if>
                <if test="item.cancelTime != null">
                    cancel_time = #{item.cancelTime},
                </if>
                <if test="item.payStatus != null">
                    pay_status = #{item.payStatus},
                </if>
                <if test="item.payMethod != null">
                    pay_method = #{item.payMethod},
                </if>
                <if test="item.checkoutTime != null">
                    checkout_time = #{item.checkoutTime},
                </if>
                <if test="item.status != null">
                    status = #{item.status},
                </if>
                <if test="item.deliveryTime != null">
                    delivery_time = #{item.deliveryTime}
                </if>
            </set>
            WHERE id = #{item.id}
        </foreach>
    </update>




    <select id="getByStatusAndOrderTimeOut" resultType="top.hazenix.order.domain.entity.Orders">
        select * from orders where status = #{pendingPayment} and order_time &lt; #{limitTime}
    </select>
    <select id="getById" resultType="top.hazenix.order.domain.entity.Orders">
        select * from orders where id = #{id}
    </select>
    <select id="sumByStatusAndOrderTime" resultType="java.lang.Double">
        select sum(amount) from orders
        <where>
            <if test="status != null">and status = #{status}   </if>
            <if test="beginTime != null">and order_time &gt;= #{beginTime}   </if>
            <if test="endTime != null">and order_time &lt;= #{endTime}   </if>
        </where>
    </select>
    <select id="getCountByDateAndStatus" resultType="java.lang.Integer">
        select count(id) from orders
        <where>
            <if test="status != null">and status = #{status}</if>
            <if test="beginTime != null">and order_time &gt;= #{beginTime}</if>
            <if test="endTime != null">and order_time &lt;= #{endTime}</if>
        </where>
    </select>
    <select id="getSalesTop10" resultType="top.hazenix.api.domain.dto.GoodsSalesDTO">
        select od.name, sum(od.number) number
        from order_detail od,orders o
        where od.order_id = o.id and o.status = 5
        <if test="beginTime != null">
            and o.order_time &gt;= #{beginTime}
        </if>
        <if test="endTime != null">
            and o.order_time &lt;= #{endTime}
        </if>
        group by od.name
        order by number desc
        limit 0,10
    </select>




    <select id="pageQuery" resultType="top.hazenix.order.domain.entity.Orders">
        select * from orders
        <where>
            <if test="number != null and number!=''">
                and number like concat('%',#{number},'%')
            </if>
            <if test="phone != null and phone!=''">
                and phone like concat('%',#{phone},'%')
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="beginTime != null">
                and order_time &gt;= #{beginTime}
            </if>
            <if test="endTime != null">
                and order_time &lt;= #{endTime}
            </if>

        </where>
        order by order_time desc
    </select>
    <select id="countByStatus" resultType="java.lang.Integer">
        select count(id) from orders where status = #{status}
    </select>
    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from orders
        <where>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="begin != null">
                and order_time &gt;= #{begin}
            </if>
            <if test="end != null">
                and order_time &lt;= #{end}
            </if>
        </where>
    </select>


</mapper>
